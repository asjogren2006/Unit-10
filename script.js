function askname() {
    var questionname = prompt("What is your name?")
    return questionname
}

alert(`Hello, ${askname()}!`)

function asktemp() {
    var questiontemp = prompt("What is the temperature?")
    return questiontemp
}

